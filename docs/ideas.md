- [ ] Придумать свой язык
- [ ] Блок ошибок (Лексер обходит, кидает ошибку синтаксическому анализатору, должен быть отделньый блок ошибок. Семантика может сообщать, если что-то будет не совпадать)
- [ ]  LL1 генератор (таблица + бегунок)
  нативые фунцкции в байткоде (подключаемые бинарники)
  глобальные переменные
  функции
  замыкания
  классы
  длинная арифметика
- 
### Планы разработки

**Фаза 1: Базовая архитектура VM и типизированный байткод**

**1.1. Проектирование OpCode**
Создать перечисление строго типизированных инструкций без динамических проверок:

* арифметические (`OP_ADD_SINGLE`, `OP_ADD_S_NUMBER`)
* побитовые (`OP_BIT_AND_U_LITTLE`)
* сравнения (включая указатели, `OP_EQ_PTR`)
* системные (`OP_PANIC`)

Все инструкции должны быть максимально специализированы и не требовать runtime-диспетчеризации типов.

**1.2. Структура Chunk**
Реализовать контейнер байткода, включающий:

* динамический массив инструкций
* пул констант (числа, строки)
* таблицу отладочной информации (соответствие инструкций строкам исходного кода)

Chunk является полностью самодостаточной единицей исполнения.

**1.3. Ядро VirtualMachine**
Реализовать:

* стек на массиве сырых 64-битных слов
* базовые операции push/pop без абстракций (`std::variant` исключён)

Добавить поддержку загрузки констант:

* `OP_CONSTANT` — числовые значения
* `OP_LOAD_STRING` — строки через пул констант

**1.4. Цикл диспетчеризации (run loop)**
Реализовать основной цикл исполнения:

* арифметика
* побитовые операции
* сравнения
* ветвления (прыжки по смещению для `if` и циклов)

Провести первичное тестирование:

* вручную собранные Chunk’и
* проверка корректности стека и переходов

**Фаза 2: Функции, память и сложные типы**

**2.1. Стек вызовов (Call Stack)**
Добавить механизм `CallFrame`:

* хранение адреса возврата
* изоляция локальных переменных
* поддержка вложенных вызовов

**2.2. Инструкции вызова**
Реализовать:

* `OP_CALL` — вызов функции
* `OP_RETURN` — возврат значения

Передача аргументов осуществляется через стек VM.

**2.3. Память структур и массивов**
Реализовать работу с кучей:

**Структуры:**

* `OP_ALLOCATE_STRUCT`
* `OP_GET_FIELD`
* `OP_SET_FIELD`
  (доступ строго по статическим смещениям)

**Динамические массивы (slices):**

* `OP_ALLOCATE_ARRAY`
* `OP_LOAD_ELEMENT`
* `OP_STORE_ELEMENT`

Все адреса и смещения рассчитываются компилятором, VM не выполняет проверок типов.

**2.4. Базис ARC**
Внедрить управление памятью через подсчёт ссылок:

* `OP_RETAIN` — увеличение счётчика
* `OP_RELEASE` — уменьшение

Поддержка:

* структур
* массивов
* строк (если они heap-allocated)

Освобождение памяти происходит при достижении счётчиком нуля.

**2.5. Нативные вызовы (FFI)**
Реализовать механизм взаимодействия с внешним миром:

* инструкция `OP_CALL_NATIVE`
* таблица маршрутизации (id → C++ функция)

Нативные функции:

* читают аргументы со стека
* выполняют системные операции (I/O, файлы, графика)
* возвращают результат обратно в VM

3: Инфраструктура ошибок и Лексер (Неделя 3)
3.1. Ядро CompilationException: Внедрить систему накопления ошибок, чтобы компиляция не прерывалась после первой проблемы.
3.2. DiagnosticEngine: Написать систему вывода ошибок в стиле Clang с кареткой (^), указывающей на точное место в исходном коде, и выводом контекста (Caret Diagnostics).
3.3. Расширение TokenType: Добавить в лексер скалярные (s_number, single), математические (vector, matrix), контейнерные типы, а также модификаторы (val, var, def).
3.4. Операторы и UFCS: Добавить лексический анализ точки (.) для UFCS, новых побитовых операций (bnd, bor, bxr, bnt) и операторов памяти (:=, <-).

4: Парсер, AST и Синтаксический сахар (Неделя 4)
4.1. Узлы AST: Создать классы для структур (struct), перечислений (enumer), функций и объявлений переменных.
4.2. Синтаксис UFCS: Реализовать парсинг цепочек методов (a.method()), преобразуя их в узлы стандартного вызова функции в дереве.
4.3. Поток выполнения: Добавить парсинг паттерн-матчинга (when), циклов (run, rep).
4.4. Panic Mode: Внедрить алгоритм синхронизации парсера для поиска точек восстановления (до ближайшей скобки или ;) при синтаксических ошибках.

5: Семантический анализатор (Sema) и Статическое разрешение (Неделя 5)
5.1. Environment & Scope: Написать SemaVisitor для отслеживания областей видимости. Расчет точных статических смещений для каждого поля в struct.
5.2. Strict Type Checking: Строгая валидация бинарных/унарных операций. Разрешение вызовов UFCS в конкретные адреса функций. Жесткий запрет неявных приведений типов.
5.3. Flattening & Lowering: Разложение (распаковка) математических векторов/матриц на серию атомарных скалярных инструкций. Трансформация цикла rep во вложенные блоки run в AST.
5.4. Fix-It Hints: Интеграция алгоритма Левенштейна. Если метод не найден, компилятор ищет похожие имена и предлагает их пользователю в тексте ошибки.

6: Управление памятью (ARC) на этапе компиляции (Неделя 6)
6.1. Lifetime Analysis: Реализовать анализ времени жизни переменных в SemaVisitor.
6.2. Авто-Retain/Release: Автоматическая вставка узлов RetainExpr при копировании умных указателей (spoint, wpoint) и ReleaseExpr при их выходе из области видимости.
6.3. Move Semantics: Проверка правил перемещения (<-) для предотвращения использования данных после их перемещения (use-after-move).
6.4. Def-константы: Вычисление значений переменных с модификатором def строго на этапе компиляции (Constant Folding).

7: Генерация байткода из AST (Неделя 7)
7.1. BytecodeGenerator: Создать визитор для перевода валидного Typed AST в байткод (Chunk).
7.2. Индексация стека: Кодогенерация для локальных переменных — перевод имен в статические индексы стека (VM больше не ищет переменные по именам).
7.3. Генерация Control Flow: Преобразование конструкций when и run в последовательности условных переходов (JumpIfFalse, Jump).
7.4. Пул констант: Связать генератор с пулом констант для загрузки чисел и литералов.

8: Бэкенд трансляции в Clight и Валидация (Неделя 8)
8.1. CCodeGeneratorVisitor: Написать бэкенд, генерирующий чистый текст стандарта C99/Clight из Typed AST.
8.2. Memory Layout Alignment: Генерация C-структур с __attribute__((aligned(X))) для полного соответствия выравниванию полей в Weasel VM.
8.3. ARC Runtime in C: Реализация на С легковесных макросов/инлайнов для retain и release, чтобы скомпилированный C-код управлял памятью аналогично нашей VM.
8.4. Интеграция и тестирование: Написать программу трассировки лучей на Weasel, выполнить её в Weasel VM, затем транслировать в Clight, скомпилировать через CompCertTSO и убедиться в побитовом совпадении результатов работы обоих бэкендов.
